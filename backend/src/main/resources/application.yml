server:
  port: 8080

spring:
  application:
    name: banco-backend
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/banco?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: banco
    password: banco123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false